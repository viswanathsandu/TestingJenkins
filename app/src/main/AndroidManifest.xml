<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.education.corsalite">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@drawable/ico_launcher"
        android:label="@string/app_name"
        tools:replace="android:icon,android:theme"
        android:theme="@style/AppTheme">

        <meta-data android:name="DATABASE" android:value="corsalite_db.db" />
        <meta-data android:name="VERSION" android:value="2" />
        <meta-data android:name="QUERY_LOG" android:value="true" />
        <meta-data android:name="DOMAIN_PACKAGE_NAME" android:value="com.education.corsalite.models.db" />

        <activity
            android:name=".activities.LoginActivity"
            android:configChanges="keyboard|keyboardHidden|layoutDirection|orientation|uiMode|screenLayout|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:configChanges="keyboard|keyboardHidden|layoutDirection|orientation|uiMode|screenLayout|screenSize"
            android:name=".activities.UserProfileActivity"
            android:theme="@style/UserProfileTheme" />
        <activity
            android:configChanges="keyboard|keyboardHidden|layoutDirection|orientation|uiMode|screenLayout|screenSize"
            android:name=".activities.WebviewActivity"
            android:label="@string/title_activity_webview" />
        <activity
            android:configChanges="keyboard|keyboardHidden|layoutDirection|orientation|uiMode|screenLayout|screenSize"
            android:name=".activities.VirtualCurrencyActivity"
            android:label="@string/title_activity_currency"
            android:theme="@style/UserProfileTheme" />

    </application>

</manifest>